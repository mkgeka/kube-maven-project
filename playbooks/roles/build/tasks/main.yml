---
# tasks file for build
- name: Build an image and push it to a private repo
  tags: build
  shell: docker build -t mkgeka/kuber-lamp-7 -f docker/Dockerfile .
  args:
    executable: /bin/bash

- name: Build an image and push it to a private repo
  tags: build
  shell: docker pull mkgeka/kuber-lamp-7
  args:
    executable: /bin/bash

