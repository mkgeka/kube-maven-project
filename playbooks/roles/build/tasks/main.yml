---
# tasks file for build
- name: Build an image and push it to a private repo
  tags: build
  community.general.docker_image:
    build:
      path: '../docker/'
    name: localhost:5000/kuber-lamp-7
    state: present
    tag: v3
    push: yes
    source: build

